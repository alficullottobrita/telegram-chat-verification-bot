{
  "name": "telegram-bot-verification",
  "version": "1.0.0",
  "description": "Telegram bot with human verification",
  "main": "public/index.html",
  "scripts": {
    "dev": "wrangler pages dev public --kv=VERIFIED_USERS --persist",
    "deploy": "wrangler pages deploy public"
  },
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "cloudflare": "^4.6.0"
  },
  "devDependencies": {
    "wrangler": "^3.1.0"
  }
}
